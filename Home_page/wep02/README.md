# 🎊 نظام إدارة محل فساتين الزفاف - الإصدار المحسّن

## نظرة عامة

نظام إدارة شامل لمحلات فساتين الزفاف والسهرة يدعم **البيع والإيجار والتصميم** مع ميزات متقدمة لإدارة دورة حياة الفواتير، المدفوعات المتعددة، منع الحجز المزدوج، والمزيد.

### ✨ الميزات الأساسية

- 📋 **إدارة كاملة للفواتير** (بيع، إيجار، تصميم)
- 👥 **إدارة العملاء** مع Timeline كامل
- 👗 **إدارة المخزون** (الفساتين والإكسسوارات)
- 📅 **نظام الحجوزات** بالتقويم
- 💰 **نظام مدفوعات متعدد المراحل**
- 📊 **تقارير وإحصائيات شاملة**
- 🔒 **نظام صلاحيات متقدم**
- 📝 **سجل كامل لجميع العمليات**

---

## 🆕 الميزات الجديدة (الإصدار 2.0)

### 1. دورة حياة الفاتورة (Invoice Lifecycle)
- ✅ 6 حالات للفاتورة: draft, reserved, out_with_customer, returned, closed, canceled
- ✅ تأكيد تسليم الفستان للعميل
- ✅ تأكيد استلام/إرجاع الفستان مع تسجيل الحالة
- ✅ إقفال الفاتورة نهائياً
- ✅ إلغاء الفاتورة (Soft Delete)
- ✅ سجل تاريخي لكل تغيير في الحالة

### 2. نظام المدفوعات المتعدد
- ✅ دفعات متعددة لكل فاتورة
- ✅ دعم 3 أنواع: دفعة، مرتجع، غرامة
- ✅ حساب تلقائي للمبلغ المتبقي
- ✅ تحديث حالة الدفع تلقائياً
- ✅ ملخص شامل للمدفوعات

### 3. منع الحجز المزدوج
- ✅ كشف تلقائي للحجوزات المتعارضة
- ✅ منع حجز نفس الفستان لفترات متداخلة
- ✅ تقويم توفر الفساتين
- ✅ دعم استثناء فاتورة معينة (للتعديل)

### 4. نظام السجلات الشامل
- ✅ تسجيل جميع العمليات في المتجر
- ✅ حفظ البيانات قبل وبعد التغيير
- ✅ تسجيل IP و User Agent
- ✅ بحث وإحصائيات متقدمة
- ✅ تصدير السجلات إلى CSV

### 5. نظام الصلاحيات المحسّن
- ✅ صلاحيات متدرجة: Admin, Manager, Staff
- ✅ حماية العمليات الحساسة
- ✅ خصومات متدرجة حسب الدور
- ✅ تحكم دقيق في الوصول

### 6. تقارير الذمم والديون
- ✅ تقرير الفواتير غير المسددة
- ✅ تقرير الأعمار (30, 60, 90+ يوم)
- ✅ رصيد كل عميل
- ✅ فلاتر متقدمة

---

## 📁 هيكل المشروع

```
wep20_pro/Home page/wep02/
│
├── migrations/                          # تحديثات قاعدة البيانات
│   └── 20251211_complete_system_features.sql
│
├── app/
│   ├── config/
│   │   └── store_database.php          # إعداد الاتصال بقاعدة بيانات المتجر
│   │
│   └── helpers/                         # مساعدين الأعمال
│       ├── invoice_lifecycle.php       # إدارة دورة حياة الفاتورة
│       ├── payment_manager.php         # إدارة المدفوعات المتعددة
│       ├── double_booking_checker.php  # منع الحجز المزدوج
│       ├── logger.php                  # نظام السجلات
│       ├── permissions.php             # نظام الصلاحيات
│       ├── customer.php                # مساعد العملاء
│       └── whatsapp.php                # تكامل WhatsApp
│
├── public/
│   ├── index.php                       # لوحة التحكم
│   ├── sales.php                       # المبيعات والفواتير
│   ├── new-invoice.php                 # فاتورة جديدة
│   ├── save_invoice.php                # حفظ الفاتورة
│   ├── customer.php                    # العملاء
│   ├── inventory.php                   # المخزون
│   ├── bookings.php                    # الحجوزات
│   ├── reports.php                     # التقارير
│   ├── settings.php                    # الإعدادات
│   │
│   └── api/                            # API Endpoints
│       ├── invoices.php                # فواتير API
│       ├── payments.php                # (جديد) مدفوعات API
│       ├── receivables.php             # (جديد) تقارير الذمم
│       ├── customers_api.php           # عملاء API
│       ├── inventory.php               # مخزون API
│       └── bookings.php                # حجوزات API
│
├── assets/
│   ├── css/                            # ملفات التنسيق
│   ├── js/                             # ملفات JavaScript
│   └── img/                            # الصور
│
├── .agent/workflows/                   # سير العمل
│   └── complete-system-features.md     # خطة التنفيذ
│
├── PROGRESS_REPORT.md                  # تقرير التقدم
├── USAGE_GUIDE.md                      # دليل الاستخدام
└── README.md                           # هذا الملف
```

---

## 🗃️ قاعدة البيانات

### الجداول الرئيسية

| الجدول | الوصف |
|--------|-------|
| `users` | الموظفون والمستخدمون |
| `customers` | العملاء |
| `categories` | فئات المنتجات |
| `suppliers` | الموردون |
| `products` | الفساتين والإكسسوارات |
| `invoices` | الفواتير |
| `invoice_items` | عناصر الفاتورة |
| `payments` | **✨ جديد** المدفوعات المتعددة |
| `invoice_status_history` | **✨ جديد** سجل تغييرات الفواتير |
| `bookings` | الحجوزات |
| `expenses` | المصروفات |
| `expense_categories` | فئات المصروفات |
| `inventory_movements` | حركة المخزون |
| `store_logs` | **✨ جديد** سجل العمليات |

### Views المحسّنة

- `active_invoices_view`: الفواتير النشطة
- `overdue_invoices_view`: الفواتير المتأخرة مع Aging
- `daily_payments_view`: ملخص المدفوعات اليومية

---

## 🚀 التثبيت والإعداد

### المتطلبات

- PHP 7.4 أو أحدث
- MySQL 5.7 أو أحدث / MariaDB 10.3 أو أحدث
- Apache/Nginx مع mod_rewrite
- مكتبة PDO لـ PHP

### خطوات التثبيت

#### 1. استنساخ/نسخ المشروع

```bash
git clone <repository_url>
cd wep20_pro/Home\ page/wep02
```

#### 2. تطبيق Migration على قاعدة البيانات

**مهم:** يجب تطبيق Migration على **كل متجر** على حدة، وليس على Master Database.

```sql
-- الاتصال بقاعدة بيانات المتجر (مثلاً: wep_store_1)
USE wep_store_1;

-- تطبيق Migration
SOURCE migrations/20251211_complete_system_features.sql;
```

**أو باستخدام PHPMyAdmin:**
1. افتح قاعدة بيانات المتجر
2. اذهب إلى تبويب "SQL"
3. الصق محتوى ملف `20251211_complete_system_features.sql`
4. اضغط "تنفيذ"

#### 3. التحقق من التطبيق

```sql
-- التحقق من الجداول الجديدة
SHOW TABLES LIKE 'payments';
SHOW TABLES LIKE 'invoice_status_history';
SHOW TABLES LIKE 'store_logs';

-- التحقق من الحقول الجديدة
DESCRIBE invoices;

-- يجب أن تظهر:
-- invoice_status, delivered_at, delivered_by, returned_at, returned_by, 
-- return_condition, return_notes, deleted_at
```

#### 4. إعداد الملفات

التأكد من وجود جميع Helper Files في `app/helpers/`:
- ✅ invoice_lifecycle.php
- ✅ payment_manager.php
- ✅ double_booking_checker.php
- ✅ logger.php
- ✅ permissions.php

---

## 📖 دليل الاستخدام

### مثال 1: إنشاء فاتورة جديدة مع التحقق من التوفر

```php
<?php
require_once __DIR__ . '/app/helpers/double_booking_checker.php';

$bookingChecker = new DoubleBookingChecker($pdo);

// التحقق من توفر الفستان
$check = $bookingChecker->checkAvailability(
    $productId = 123,
    $collectionDate = '2025-12-15',
    $returnDate = '2025-12-20'
);

if (!$check['available']) {
    echo "تحذير: الفستان محجوز في هذه الفترة!";
    foreach ($check['conflicts'] as $conflict) {
        echo "- فاتورة {$conflict['invoice_number']} للعميل {$conflict['customer_name']}";
    }
    exit;
}

// المتابعة بإنشاء الفاتورة...
?>
```

### مثال 2: تسليم الفستان للعميل

```php
<?php
require_once __DIR__ . '/app/helpers/invoice_lifecycle.php';

$lifecycleManager = new InvoiceLifecycleManager($pdo);

try {
    $result = $lifecycleManager->deliverInvoice(
        $invoiceId = 456,
        $userId = $_SESSION['user_id'],
        $notes = "تم التسليم في الموعد المحدد"
    );
    
    echo $result['message'];
    // حالة الفاتورة الآن: out_with_customer
    
} catch (Exception $e) {
    echo "خطأ: " . $e->getMessage();
}
?>
```

### مثال 3: إضافة دفعة جديدة

```php
<?php
require_once __DIR__ . '/app/helpers/payment_manager.php';

$paymentManager = new PaymentManager($pdo);

try {
    $result = $paymentManager->addPayment(
        $invoiceId = 456,
        $amount = 500.00,
        $method = 'cash',
        $type = 'payment',
        $notes = "دفعة من العميل",
        $userId = $_SESSION['user_id']
    );
    
    echo "تم إضافة الدفعة. المتبقي: " . $result['new_remaining'] . " ريال";
    
} catch (Exception $e) {
    echo "خطأ: " . $e->getMessage();
}
?>
```

**للمزيد من الأمثلة، راجع `USAGE_GUIDE.md`**

---

## 🔐 الصلاحيات

### الأدوار المتاحة

| الدور | الصلاحيات |
|-------|-----------|
| **Admin** | جميع الصلاحيات |
| **Manager** | كل شيء ما عدا: حذف/تعديل المدفوعات، إدارة المستخدمين، عرض السجلات |
| **Staff** | الصلاحيات الأساسية فقط (عرض، إنشاء، تعديل) |

### أمثلة على الصلاحيات

```php
$permissions = new PermissionsManager($pdo, $_SESSION['user_id']);

// Admin فقط
if ($permissions->canDeletePayment()) { /*...*/ }
if ($permissions->canManageUsers()) { /*...*/ }
if ($permissions->canViewLogs()) { /*...*/ }

// Manager أو أعلى
if ($permissions->canCancelInvoice()) { /*...*/ }
if ($permissions->canViewProductCost()) { /*...*/ }
if ($permissions->canViewProfitReports()) { /*...*/ }

// الجميع
if ($permissions->can('create_invoice')) { /*...*/ }
```

---

## 📊 التقارير المتوفرة

### 1. تقارير المبيعات
- المبيعات اليومية/الأسبوعية/الشهرية
- توزيع أنواع العمليات (بيع، إيجار، تصميم)
- رسوم بيانية

### 2. تقارير المدفوعات
- المدفوعات اليومية
- تجميع حسب طريقة الدفع
- المرتجعات والغرامات

### 3. تقارير الذمم (جديد)
- الفواتير غير المسددة
- تقرير الأعمار (30, 60, 90+ يوم)
- رصيد كل عميل
- إجمالي الذمم

### 4. تقارير الحجوزات
- الحجوزات اليومية/الأسبوعية
- توزيع أنواع الحجوزات
- حالات الحجوزات

### 5. تقارير المخزون
- المنتجات المتوفرة
- المنتجات المحجوزة
- المنتجات ذات المخزون المنخفض

---

## 🐛 استكشاف الأخطاء

### المشكلة: Migration يفشل

**الحل:**
1. تأكد من أنك متصل بقاعدة بيانات المتجر (wep_store_X)
2. تأكد من أن المستخدم لديه صلاحيات ALTER TABLE
3. تحقق من سجل الأخطاء (error log)

### المشكلة: Helper Class لا يعمل

**الحل:**
1. تأكد من وجود الملف في `app/helpers/`
2. تحقق من `require_once` في أعلى الملف
3. تأكد من أن `$pdo` معرّف ومتصل

### المشكلة: الصلاحيات لا تعمل

**الحل:**
1. تحقق من أن `user_id` موجود في Session
2. تحقق من أن دور المستخدم محدد في جدول `users`
3. تأكد من استخدام `PermissionsManager` بشكل صحيح

---

## 🔄 التحديثات المستقبلية (Roadmap)

### المرحلة القادمة (قيد التطوير)

- [ ] API endpoints للميزات الجديدة
- [ ] واجهات مستخدم محسّنة
- [ ] صفحة تفاصيل الفاتورة المتقدمة
- [ ] تقارير الذمم بواجهة كاملة
- [ ] تقويم الحجوزات التفاعلي
- [ ] Customer Timeline كامل
- [ ] نظام التنبيهات والإشعارات

### ميزات مخطط لها

- Barcode/QR Code للفواتير
- Email notifications
- SMS integration
- Mobile app
- نظام الخصومات والعروض
- تقارير الربحية المتقدمة

---

## 🤝 المساهمة

نرحب بالمساهمات! يرجى اتباع الخطوات التالية:

1. Fork المشروع
2. إنشاء branch جديد (`git checkout -b feature/amazing-feature`)
3. Commit التغييرات (`git commit -m 'Add amazing feature'`)
4. Push إلى Branch (`git push origin feature/amazing-feature`)
5. فتح Pull Request

---

## 📝 الترخيص

هذا المشروع مرخص بموجب [اسم الترخيص] - راجع ملف LICENSE للتفاصيل.

---

## 👨‍💻 المطورون

- **الفريق الأساسي** - *Deepmind Advanced Agentic Coding*
- **المساهمون** - راجع قائمة المساهمين

---

## 📞 الدعم

للدعم الفني أو الاستفسارات:
- البريد الإلكتروني: support@example.com
- الوثائق: [رابط الوثائق]
- المنتدى: [رابط المنتدى]

---

## 📚 روابط مفيدة

- [دليل الاستخدام الكامل](USAGE_GUIDE.md)
- [تقرير التقدم](PROGRESS_REPORT.md)
- [خطة التنفيذ](. agent/workflows/complete-system-features.md)
- [تقرير التحليل الشامل](تقرير_تحليل_النظام_الشامل.md)

---

**الإصدار:** 2.0
**تاريخ آخر تحديث:** ٢٠٢٥-١٢-١١
**الحالة:** نشط - قيد التطوير المستمر

---

<div align="center">
  
### صُنع بـ ❤️ لمحلات فساتين الزفاف

</div>
