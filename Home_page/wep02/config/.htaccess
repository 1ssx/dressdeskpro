# =======================================================
# BLOCK ALL ACCESS TO CONFIG DIRECTORY
# This directory contains configuration files with
# sensitive database credentials
# =======================================================

# Deny all access
Order deny,allow
Deny from all

# Alternative method using mod_rewrite
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule .* - [F,L]
</IfModule>
