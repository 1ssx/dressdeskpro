# =======================================================
# BLOCK ALL ACCESS TO APP DIRECTORY
# This directory contains application code and should
# NEVER be accessible via web browser
# =======================================================

# Deny all access
Order deny,allow
Deny from all

# Alternative method using mod_rewrite
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule .* - [F,L]
</IfModule>
