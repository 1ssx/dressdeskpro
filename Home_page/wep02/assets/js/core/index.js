/**
 * Core Utilities Loader
 * PHASE 4 - JS Restructuring
 * 
 * Loads all core utilities for use across the application
 * Include this file before other modules
 */

// Load core utilities (they're already in global scope when loaded via <script> tags)
// This file serves as documentation and can be used for module bundling in the future

// Core utilities available:
// - api-client.js: apiGet, apiPost, apiPut, apiDelete, handleApiError
// - formatters.js: formatCurrency, formatNumber, formatArabicDate, formatTimeAgo, etc.
// - dom-utils.js: $, $$, createElement, show, hide, toggle, etc.
// - validators.js: isValidEmail, isValidPhone, validateForm, etc.

console.log('Core utilities loaded');

