# =======================================================
# BLOCK ALL ACCESS TO TEMP PDFS DIRECTORY
# This directory contains temporary PDF files
# that should only be accessed by server-side code
# =======================================================

# Deny all access
Order deny,allow
Deny from all

# Alternative method using mod_rewrite
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule .* - [F,L]
</IfModule>
