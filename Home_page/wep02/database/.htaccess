# =======================================================
# BLOCK ALL ACCESS TO DATABASE DIRECTORY
# This directory may contain database-related files
# =======================================================

# Deny all access
Order deny,allow
Deny from all

# Alternative method using mod_rewrite
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule .* - [F,L]
</IfModule>
